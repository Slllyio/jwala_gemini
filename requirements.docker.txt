# ============================================================
# Docker-only requirements (no ROCm/GPU — CPU inference only)
# For GPU training, use the host .venv with ROCm wheels
# ============================================================

# Google Earth Engine
earthengine-api>=0.1.390
geemap>=0.28.0

# HuggingFace / Model
huggingface_hub>=0.20.3
transformers>=4.37.0
timm>=0.9.12
# Phase 3: TerraTorch for Prithvi-EO-2.0 (optional — falls back to 100M if absent)
terratorch>=0.1.0

# PyTorch CPU (Docker)
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Geospatial
rasterio>=1.3.9
geopandas>=0.14.3
pyproj>=3.6.1
shapely>=2.0.3
fiona>=1.9.5

# Data handling
numpy==1.26.4
pandas>=2.1.4
scikit-learn>=1.3.2
scipy>=1.12.0

# ML
lightgbm>=4.2.0

# Visualization
folium>=0.15.1
matplotlib>=3.8.2
seaborn>=0.13.1
plotly>=5.18.0

# Utilities
tqdm>=4.66.1
pyyaml>=6.0.1
einops>=0.7.0
Pillow>=10.2.0

# Database
psycopg[binary]>=3.1.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0          # used by API tests + async health probes

# PDF generation (beat reports)
reportlab>=4.2.0

# Monitoring & Notifications
requests>=2.31.0
python-json-logger>=2.0.7

# Notebooks (Jupyter service)
jupyter>=1.0.0
ipywidgets>=8.1.1
